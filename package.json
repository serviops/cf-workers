{
  "name": "cf-workers",
  "version": "1.0.2",
  "description": "An example worker to demonstrate multiple CICD integrations",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write --no-semi --single-quote --print-width 100 --trailing-comma es5 '**/*.{js,css,json,md}'"
  },
  "author": "Stephanie",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.8.1",
    "serverless": "^3.25.1",
    "serverless-cloudflare-workers": "^1.2.0"
  }
}
