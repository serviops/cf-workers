{
  "name": "cf-workers",
  "version": "1.0.2",
  "description": "An example worker to demonstrate multiple CICD integrations",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write --no-semi --single-quote --print-width 100 --trailing-comma es5 '**/*.{js,css,json,md}'"
  },
  "author": "Stephanie Guilbert",
  "repository": "https://github.com/serviops/cf-workers",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "@cloudflare/wrangler": "^1.8.4"
  }
}
